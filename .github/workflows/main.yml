name: Build and Push Docker Image to ECR

on:
  push:
    branches:
      - devops-ben
      - workflow_dispatch

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
